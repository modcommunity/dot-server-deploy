Any content here is loaded by every "game" instance on the server.

Nothing uses it yet. It is scanned and skipped rather than treated as a game,
because a directory with no `game.yml` is exactly what a half-finished upload
looks like and an operator should be able to tell the two apart.
